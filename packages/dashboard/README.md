# @opsclaw/dashboard

Mission Control frontend foundation package.

## Scope (Phase 4 Slice 04-02)

- Org hierarchy panel
- Agent profile panel
- Activity feed panel
- Deterministic view-model helpers with runnable tests

## Commands

- Run tests: `npm --prefix packages/dashboard test`
